using System.Collections.Generic;
using System.Data;
using System.Threading.Tasks;

namespace {{ Namespace }};

public interface IRepository<T>
    where T : class
{
    Task<IEnumerable<T>> GetAllAsync();
    Task<IEnumerable<T>> GetAllAsync(IDbConnection connection, IDbTransaction transaction);
    Task<T?> GetByIdAsync(string id);
    Task<T?> GetByIdAsync(string id, IDbConnection connection, IDbTransaction transaction);
    Task<int> AddAsync(T obj);
    Task<int> AddAsync(T obj, IDbConnection connection, IDbTransaction transaction);
    Task<int> UpdateAsync(T obj);
    Task<int> UpdateAsync(T obj, IDbConnection connection, IDbTransaction transaction);
    Task<int> DeleteAsync(T obj);
    Task<int> DeleteAsync(T obj, IDbConnection connection, IDbTransaction transaction);
}